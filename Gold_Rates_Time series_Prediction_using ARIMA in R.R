library(forecast)
setwd("C://Users//ARAVIND PC//Documents//R_codes//2. Gold_Rates_Time series in R")
gold = read.csv("goldrates2019.txt",sep='\t')
str(gold)
head(gold)
tseries <- ts(gold$Price, start = c(1950,1), end = c(2019,8), frequency = 12)
str(tseries)
plot(tseries, xlab='Year', ylab='Gold rate in US$')
plot(decompose(tseries))
library(tseries)
#Augmented Dickey-Fuller hypothesis test
adf.test(tseries, alternative="stationary", k=12)
findbest <- auto.arima(tseries)
plot(forecast(findbest,h=24))
